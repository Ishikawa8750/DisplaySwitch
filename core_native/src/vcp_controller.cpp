/**
 * DisplaySwitch Native — VCP Controller (shared interface)
 * DDC/CI brightness / input source read/write stubs.
 */
#include "displayswitch/display_detector.h"

// Platform implementations live in platform/windows/ and platform/macos/.
// This file is intentionally empty; it exists so CMake has a valid source.
